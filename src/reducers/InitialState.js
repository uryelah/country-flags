const subState = {
  loading: false,
  error: null,
  subscription: null,
  item: null,
};

const groupState = {
  groupBy: [1, 2, 3, 4, 5, 6],
  filter: null,
  sorting: 1,
  dark: false,
};

export { subState, groupState };